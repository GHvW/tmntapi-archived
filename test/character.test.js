const chai = require("chai");
const chaiHttp = require("chai-http");
const server = reqiure("../server");

const assert = chai.assert;

describe("character endpoint tests", () => {
  describe("GET all characters", () => {
    it("should get all characters")
  })
})
