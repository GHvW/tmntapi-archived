const chai = require("chai");
const server = require("../src/server");
const assert = require("assert");

// const assert = chai.assert;

describe("/GET API", () => {
  it("checks equality for now", () => {
    assert.equal(true, true);
  });
});
